#-*- coding:utf-8 -*-
#python程序前加上一行代码，可以保证代码中中文在python2下运行不报错
print("hello world")

#input后输入的内容默认为字符串类型，即str
#可将其转化为整型，即int；
age = input("请输入年龄：")
age = int(age)#将字符串类型转换为整型；
if age > 18:
    print("已经成年，可以自由出入网吧！")

print("xxx")默认执行后换行，print("XXX",end = "")执行后不换行

print("")表示执行后换行

print("ha\tha")  打印效果：ha   ha,  \t相当于tab键，达到对齐效果；

print("\n")  \n达到换行效果；

字典查找用info.get(XXX),字典里没有XXX不会异常，用info['xxx']如果字典里没有xxx则程序异常
